import { CheckoutForm } from "@/components/store/checkout-form";

export const metadata = {
  title: "Checkout",
  description: "Complete your ZAVÉLIA order via WhatsApp.",
};

export default function CheckoutPage() {
  return <CheckoutForm />;
}
