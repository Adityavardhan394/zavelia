import { CartView } from "@/components/store/cart-view";

export const metadata = {
  title: "Bag",
  description: "Your ZAVÉLIA shopping bag.",
};

export default function CartPage() {
  return <CartView />;
}
